R5T.Coventry - An opinionated application startup types library.

Application startup adds:

* Default and configuration name-specific JSON AppSettings files.
* Console Logging.
* Configuration name provider service (direct from configuration).

